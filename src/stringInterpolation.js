// String Interpolation in ES5
function printCoord(x, y) {
    console.log('('+x+', '+y+')');
}

// String interperation in ES6 using template literals 
function printCoord(x, y) {
  console.log(`(${x}, ${y})`);
}